---
- name: Include variables
  include_vars: "vars/{{ ansible_distribution }}.yml"
  tags: always

- name: Import install tasks
  import_tasks: "install_{{ ansible_distribution }}.yml"
  tags:
  - nginx
  - install_nginx
